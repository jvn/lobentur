<h1>Listing aktivitets</h1>

<table>
  <tr>
    <th>Titel</th>
    <th>Formal</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @aktivitets.each do |aktivitet| %>
  <tr>
    <td><%= aktivitet.titel %></td>
    <td><%= aktivitet.formal %></td>
    <td><%= link_to 'Show', aktivitet %></td>
    <td><%= link_to 'Edit', edit_aktivitet_path(aktivitet) %></td>
    <td><%= link_to 'Destroy', aktivitet, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Aktivitet', new_aktivitet_path %>
